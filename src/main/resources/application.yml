server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/farm_db?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
